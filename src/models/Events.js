export default {
  
  /**
   * User selected a new diary. Payload is diary object.
   */
  CURRENT_DIARY_CHANGED: 'CURRENT_DIARY_CHANGED',

  /**
   * Current date changed. Payload is the object containing current date
   * and links the previous and next date
   */
  CURRENT_DATE_CHANGED: 'CURRENT_DATE_CHANGED'

}
